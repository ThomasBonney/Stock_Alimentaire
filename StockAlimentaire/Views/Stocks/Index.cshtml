@model IEnumerable<StockAlimentaire.Models.Stock>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.utilisateur_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.utilisateur_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.stock_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.stock_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.stock_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
